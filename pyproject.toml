[project]
name = "unitconverter-10"
version = "0.1.0"
description = "UnitConverter_10 — ECB + Dual-Track TDD harness"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
